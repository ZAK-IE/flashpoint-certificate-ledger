{
  "name": "flashpoint-certificate-ledger",
  "version": "1.0.0",
  "private": true,
  "description": "Hash-chained certificate registry for training institutes — proof of concept",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/migrate.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
